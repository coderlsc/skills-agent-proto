# Skills Agent 环境变量配置
# 复制此文件为 .env 并填入你的配置


# ==================== 认证配置 ====================

# AUTH_TOKEN (api key)
ANTHROPIC_AUTH_TOKEN=sk_xxxx

# 自定义 API Base URL（用于代理或自定义端点）
ANTHROPIC_BASE_URL=https://api.jiekou.ai/anthropic


# ==================== 模型配置 ====================

# 默认使用的模型（可选值: claude-sonnet-4-20250514, claude-opus-4-20250514 等）
CLAUDE_MODEL=claude-opus-4-5-20251101


# ==================== 会话历史存储配置 ====================

# Checkpoint 存储类型: memory (默认) 或 mysql
# memory: 内存存储，程序重启后历史丢失
# mysql: MySQL 持久化存储，程序重启后历史保留
SKILLS_CHECKPOINT_TYPE=mysql

# MySQL 连接配置（仅当 SKILLS_CHECKPOINT_TYPE=mysql 时需要）
SKILLS_MYSQL_HOST=localhost
SKILLS_MYSQL_PORT=3306
SKILLS_MYSQL_USER=root
SKILLS_MYSQL_PASSWORD=123456
SKILLS_MYSQL_DATABASE=langchain_skills

# MySQL 连接池配置（可选）
# SKILLS_MYSQL_POOL_SIZE=5
# SKILLS_MYSQL_MAX_OVERFLOW=10


# ==================== MySQL 初始化说明 ====================
#
# 方式一：一键初始化（推荐）
#   uv run python scripts/init_all.py
#
# 方式二：仅创建数据库
#   mysql -u root -p < scripts/init_database.sql
#   # 然后启动 Agent，表会自动创建
#
# 方式三：Agent 首次启动时自动创建
#   1. 先创建数据库（见方式二）
#   2. 启动 Agent: uv run langchain-skills --interactive
#   3. 表会自动创建
#
# 数据库重置：
#   mysql -u root -p < scripts/reset_database.sql
#
# ==================== 其他配置 ====================

# 最大对话轮数限制
MAX_TURNS=20

# 权限模式 (default, acceptEdits, bypassPermissions)
PERMISSION_MODE=bypassPermissions
